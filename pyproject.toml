[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "weighted-batcher"
version = "0.1.0"
description = "Weighted sampler and exact metric serialisation"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["weighted_batcher"]
